export type Product = {
  name: string;
  slug: string;
  image: string;
  category: string;
  brand: string;
  price: number;
  currency: string;
  stockCount: string;
  descriptions: string;
  rating: number;
};
